import { CheckCircle } from '@phosphor-icons/react';
import styled from 'styled-components';
import { themes } from '../../styles/themes';

export const CorrectIcon = styled(CheckCircle)`
  color: ${themes.default.green['500']}
`;