import { XCircle } from '@phosphor-icons/react';
import styled from 'styled-components';
import { themes } from '../../styles/themes';

export const FailIcon = styled(XCircle)`
  color: ${themes.default.red['600']}
`;